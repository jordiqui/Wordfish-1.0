diff --git a/src/uci.cpp b/src/uci.cpp
index 0000000..0000001 100644
--- a/src/uci.cpp
+++ b/src/uci.cpp
@@ -1,6 +1,18 @@
 /*
   Stockfish, a UCI chess playing engine derived from Glaurung 2.1
   Copyright (C) 2004-2025 The Stockfish developers (see AUTHORS file)
+*/
+
+// ---------------------------------------------------------------------------
+// Engine identity (single source of truth for GUIs)
+// These are fallbacks; your Makefile can override them, e.g.:
+//   CXXFLAGS += -DENGINE_NAME='"Wordfish 1.0.1 dev"' -DENGINE_BUILD_DATE='"250822"'
+// ---------------------------------------------------------------------------
+#ifndef ENGINE_NAME
+#define ENGINE_NAME "Wordfish 1.0.1 dev"
+#endif
+#ifndef ENGINE_BUILD_DATE
+#define ENGINE_BUILD_DATE "250822"
+#endif
-*/
 
 #include "uci.h"
 
@@ -119,10 +131,12 @@ void UCIEngine::loop() {
 
         else if (token == "uci")
         {
-            sync_cout << "id name " << engine_info(true) << "\n"
-                      << engine.get_options() << sync_endl;
+            // Force a stable, explicit UCI name so GUIs show "Wordfish 1.0.1 dev <date>"
+            sync_cout << "id name " << ENGINE_NAME << ' ' << ENGINE_BUILD_DATE << "\n"
+                      << "id author Jorge Ruiz" << "\n"
+                      << engine.get_options() << sync_endl;
 
             sync_cout << "uciok" << sync_endl;
         }
@@ -371,8 +385,9 @@ void UCIEngine::benchmark(std::istream& args) {
 
     // clang-format off
 
-    std::cerr << "==========================="
-              << "\nVersion                    : Wordfish 1.0 120825 " << __DATE__ << " " << __TIME__
+    std::cerr << "==========================="
+              << "\nVersion                    : " << ENGINE_NAME << ' ' << ENGINE_BUILD_DATE
+              << ' ' << __DATE__ << ' ' << __TIME__
               << compiler_info()
               << "Large pages                  : " << (has_large_pages() ? "yes" : "no")
               << "\nUser invocation            : " << BenchmarkCommand << " "
